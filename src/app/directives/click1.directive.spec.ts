import { Click1Directive } from './click1.directive';

describe('Click1Directive', () => {
  it('should create an instance', () => {
    const directive = new Click1Directive();
    expect(directive).toBeTruthy();
  });
});
